# Copy this file to `.env` (same folder as pubspec.yaml) and fill in real values.
# .env is already gitignored — never commit real keys.

# Kitty AI — Google Gemini free tier (no card required).
# Get a key at https://aistudio.google.com/apikey
# In AI Studio, restrict the key to your app's Android package name / iOS
# bundle ID before shipping — it's bundled client-side per your setup choice.
GEMINI_API_KEY=
